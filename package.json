{
  "name": "bit-linguagem",
  "version": "1.3.0",
  "private": false,
  "description": "BIT — linguagem textual brasileira para criação de jogos 2D.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "verify": "node scripts/verify-bit.mjs"
  },
  "dependencies": {
    "@google/genai": "^2.22.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  }
}
